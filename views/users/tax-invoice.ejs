<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-GLhlTQ8iRABdZLl6O3oVMWSktQOp6b7In1Zl3/Jr59b6EGGoI1aFkw7cmDA6j6gD" crossorigin="anonymous">
  <title>Document</title>
</head>
<body class="mt-5">
  <div class="container-fluid">
    <div class="row">
      <div class="col-12 text-center">
        <h2>Tax Invoice</h2>
      </div>
      <div class="col-12">
        <p class="fw-bold">Sold By: Kroma Corporations Pvt Ltd</p>
      </div>
      <hr>
      <div class="col-12 row">
        <div class="col-4">
          <p class="fw-bold">Order ID: <%= order._id %> </p>
          <p>Order Date: <%= new Date(order.createdAt).toLocaleDateString("en-GB") %> </p>
          <p>Invoice Date: <%= new Date().toLocaleDateString("en-GB") %> </p>
        </div>
        <div class="col-4">
          <p class="fw-bold">Bill To:</p>
          <p class="fw-bold"> <%= user.fname + ' ' + user.lname %> </p>
          <p><%= order.address.street1 + " " + order.address?.street2 + " " + order.address.city + " " + order.address.state + " " + order.address.zip %> </p>
          <p><%= user.phone %> </p>
        </div>
        <div class="col-4">
          <p class="fw-bold">Ship To:</p>
          <p class="fw-bold"><%= user.fname + ' ' + user.lname %></p>
          <p><%= order.address.street1 + " " + order.address?.street2 + " " + order.address.city + " " + order.address.state + " " + order.address.zip %></p>
          <p><%= user.phone %></p>
        </div>
      </div>
      <hr>
      <div class="col-12 row">
        <table>
          <thead>
            <tr>
              <th class="fw-bold">Title</th>
              <th class="fw-bold">Color</th>
              <th class="fw-bold">Quantity</th>
              <th class="fw-bold">Total</th>
            </tr>
          </thead>
          <tbody>
            <% order.items.forEach(item => { %>
              <tr>
                <td><%= item.productName %></td>
                <td><%= item.color %></td>
                <td><%= item.quantity %></td>
                <td>₹<%= item.quantity * item.price %></td>
              </tr>
            <% }) %>  
          </tbody>
        </table>
        <hr>
        <div>
          <div class="d-flex flex-row justify-content-end">
            <h3>Grand Total</h3>
            <h3 class="ms-5">₹<%= order.totalAmount %> </h3>
          </div>
        </div>
      </div>
    </div>
  </div>
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js" integrity="sha384-w76AqPfDkMBDXo30jS1Sgez6pr3x5MlQ1ZAGC+nuZB+EYdgRZgiwxhTBTkF7CXvN" crossorigin="anonymous"></script>
</body>
</html>